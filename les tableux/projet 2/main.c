#include <stdio.h>
#include <stdlib.h>

int main()
{
    int nb ;
      printf("entrer le nb : ");
      scanf("%d",&nb]);
     int T[nb];
     int i ;
     for (i=0 ; i<nb ; i++){
        printf("entrer element %d",i+1);
              scanf("%d",&T[i]);

     }

        for (i=0 ; i<nb ; i++){
            printf("%d\n",T[i]);
        }

    return 0;
}
